import React from "react";

const Course = ({ course }) => {
  const { image, title, desc, price, credit } = course;
  return (
    <div>
      <h1>Course</h1>
    </div>
  );
};

export default Course;
